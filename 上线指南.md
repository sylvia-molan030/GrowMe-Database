# GrowMe 上线指南

## 第一步：push 代码到 GitHub

```bash
cd /Users/sylvia/Desktop/sylvia/growme
git push origin main
```

- Username：`sylvia-molan030`
- Password：GitHub Token（https://github.com/settings/tokens 生成，勾选 **repo**）

## 第二步：开启 Pages

打开：https://github.com/sylvia-molan030/GrowMe-Database/settings/pages

| 选项 | 选择 |
|------|------|
| Source | Deploy from a branch |
| Branch | main |
| 文件夹 | **/docs** |

点 Save，等 1～3 分钟。

## 第三步：访问网站

https://sylvia-molan030.github.io/GrowMe-Database/

## 以后更新数据

数据分两条线，**不要混用**：

| 你给的文件 | 用这条命令 | 会更新的栏目 |
|-----------|-----------|-------------|
| 账户全量 CSV（如 `Pingme_GrowMe_*.csv`） | `./scripts/update_account.sh` | 黄金交叉、排行榜、设计师、资产库 → **账户内** |
| 周度 WW 文件（如 `0615周WW的数据.csv`） | `./scripts/update_weekly.sh` | **周维度更新**、**回滚素材** + 上述四栏 → **上新素材成效** |

> 只给全量数据时，**周维度不会变**。只给周度文件时，**账户内不会变**。

### 本地网页上传（推荐预览）

```bash
cd /Users/sylvia/Desktop/sylvia/growme && ./start.sh
```

浏览器打开 http://localhost:8000/ ，点右上角 **↑ 上传数据**，选择 CSV/Excel 即可。

| 文件类型 | 命名示例 | 更新范围 |
|---------|---------|---------|
| 账户全量 | `Pingme_GrowMe_*.csv` → 存为 `account_all_WW.csv` | 账户内 4 个栏目 |
| 周度 WW | `0615周WW的数据.csv` | 周维度 + 上新成效 + 可回滚推荐 |
| 数字人 | `0608周数字人.csv` | 周维度内新方向测试 |
| 历史回滚 | `0608-0615周回滚素材.csv` | 回滚素材·历史 |

> **GitHub 线上站**是静态快照，无法直接上传；更新线上仍用下方脚本 push。

### 更新账户全量（账户内 4 个栏目）

```bash
./scripts/update_account.sh --push ~/Downloads/Pingme_GrowMe_082501_*.csv
```

目标文件固定为 `data_inputs/account_all_WW.csv`。

### 更新周度上新（周维度 + 上新成效 + 回滚素材）

把 Meta 导出的周度文件放进项目，文件名须含 **周** 或 **week**，例如：

- `0608周WW的数据.csv` — 周度上新
- `0608周数字人.csv` — 数字人新方向（单独跟踪）
- `0608-0615周回滚素材.csv` — 历史回滚成效（可选，有则更新）

> 周度上新 **仅导入 WW**，T1 周度文件不再加载。

**回滚素材说明：**
- **可回滚推荐**：每次导入新周 WW 数据后，自动从**最新一周**筛选「消耗 < $200 且有购物」的素材
- **历史回滚**：需单独提供 `*回滚素材.csv` 文件

```bash
# 仅周度
./scripts/update_weekly.sh --push ~/Downloads/0615周WW的数据.csv

# 周度 + 回滚历史一并导入
./scripts/update_weekly.sh --push ~/Downloads/0615周WW的数据.csv ~/Downloads/0608-0615周回滚素材.csv
```

### 仅重建不上传

```bash
./scripts/update_account.sh    # 账户内
./scripts/update_weekly.sh     # 周维度
```

### 手动步骤

```bash
python3 scripts/build_static.py
git add data_inputs/ docs/data/
git commit -m "更新数据"
git push
```
