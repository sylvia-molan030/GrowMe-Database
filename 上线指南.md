# GrowMe 上线指南

## 第一步：push 代码到 GitHub

```bash
cd /Users/sylvia/Desktop/sylvia/growme
git push origin main
```

- Username：`sylvia-molan030`
- Password：GitHub Token（https://github.com/settings/tokens 生成，勾选 **repo**）

## 第二步：开启 Pages

打开：https://github.com/sylvia-molan030/GrowMe-Database/settings/pages

| 选项 | 选择 |
|------|------|
| Source | Deploy from a branch |
| Branch | main |
| 文件夹 | **/docs** |

点 Save，等 1～3 分钟。

## 第三步：访问网站

https://sylvia-molan030.github.io/GrowMe-Database/

## 以后更新数据

### 每周上新（推荐一条命令）

把 Meta 导出的周度文件放进项目，文件名须含 **周** 或 **week**，例如：

- `0608周WW的数据.csv`
- `0608周T1的数据.csv`
- `0608周数字人.csv`

```bash
# 从 Downloads 复制 + 重建 + 推送线上（周维度 + 上新素材成效 一并更新）
./scripts/update_weekly.sh --push ~/Downloads/0615周WW的数据.csv ~/Downloads/0615周T1的数据.csv

# 或文件已在 data_inputs/ 里，只重建并推送
./scripts/update_weekly.sh --push
```

**会自动更新：**
- 周维度更新（新周 Tab + KPI + 洞察）
- 黄金交叉 / 排行榜 / 设计师 → 切换「上新素材成效」
- 核心资产晋级库 →「上新素材成效」

### 仅重建不上传

```bash
./scripts/update_weekly.sh
```

### 手动步骤

```bash
python3 scripts/build_static.py
git add data_inputs/ docs/data/
git commit -m "更新数据"
git push
```
