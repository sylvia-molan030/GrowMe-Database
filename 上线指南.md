# GrowMe 上线指南

## 推荐：Cloudflare Pages（不依赖 GitHub Actions）

GitHub Pages 部署若卡在 queued/pending，可改用 Cloudflare，**构建在 Cloudflare 侧完成**，不受 GitHub Actions 故障影响。

### 一次性配置

1. 打开 https://dash.cloudflare.com/ → **Workers & Pages** → **Create** → **Pages** → **Connect to Git**
2. 选择仓库 **sylvia-molan030/GrowMe-Database**
3. 构建设置：

| 选项 | 值 |
|------|-----|
| Production branch | `main` |
| Framework preset | None |
| Build command | *(留空)* |
| Build output directory | **`docs`** |

4. **Save and Deploy**

部署完成后地址形如：`https://growme-database.pages.dev`

### 可选：自定义域名

Pages 项目 → **Custom domains** → 添加你自己的域名。

### 可选：GitHub Actions 直推 Cloudflare

若需用 API 直推，可参考 Cloudflare 文档自行添加 workflow；需 Token 含 **workflow** 权限。  
**更推荐**上面 Dashboard 连 Git 的方式，无需 GitHub Actions。

---

## 备选：Netlify

1. 打开 https://app.netlify.com/ → **Add new site** → **Import an existing project** → GitHub → **GrowMe-Database**
2. 构建设置（仓库根目录已有 `netlify.toml`，一般自动识别）：

| 选项 | 值 |
|------|-----|
| Branch | `main` |
| Build command | *(留空或 echo)* |
| Publish directory | **`docs`** |

3. **Deploy site**

地址形如：`https://growme-database.netlify.app`

---

## 原方案：GitHub Pages（备用）

```bash
cd /Users/sylvia/Desktop/sylvia/growme
git push origin main
```

Settings → Pages → Branch **main** / 文件夹 **/docs**

https://sylvia-molan030.github.io/GrowMe-Database/

> GitHub Actions 故障时 Pages 可能长时间不更新，优先用 Cloudflare/Netlify。

---

## 第一步：push 代码到 GitHub

```bash
cd /Users/sylvia/Desktop/sylvia/growme
git push origin main
```

- Username：`sylvia-molan030`
- Password：GitHub Token（https://github.com/settings/tokens 生成，勾选 **repo**）

---

## 以后更新数据

数据分两条线，**不要混用**：

| 你给的文件 | 用这条命令 | 会更新的栏目 |
|-----------|-----------|-------------|
| 账户全量 CSV（如 `Pingme_GrowMe_*.csv`） | `./scripts/update_account.sh --push` | 黄金交叉、排行榜、设计师、资产库 → **账户内** + **上新素材成效** |
| 周度 WW 文件（如 `0713周老素材.csv`） | `./scripts/update_weekly.sh --push` | **周维度更新**、**回滚素材** + 上新成效 |

> 只给全量数据时，**周维度 Tab 不会新增**。只给周度文件时，**账户内全量不会变**。

### 本地网页上传（推荐预览）

```bash
cd /Users/sylvia/Desktop/sylvia/growme && ./start.sh
```

浏览器打开 http://localhost:8000/ ，点右上角 **↑ 上传数据**。

| 文件类型 | 命名示例 | 更新范围 |
|---------|---------|---------|
| 账户全量 | `*全数据*.csv` → `account_all_WW.csv` | 账户内 + 上新成效 |
| 周度老素材 | `0713周老素材.csv` | 周维度 · 老方向 |
| 周度新创意 | `0713周新创意素材.csv` | 周维度 · 新方向 |
| 周度图片 | `0713周图片素材.csv` | 周维度 · 图片 |
| 历史回滚 | `*回滚素材*.csv` | 回滚素材 |

> **Cloudflare / Netlify 线上站**是静态快照；更新线上用 `--push` 脚本即可自动同步。

### 更新账户全量

```bash
./scripts/update_account.sh --push ~/Downloads/全数据更新-0715.csv
```

### 更新周度上新

```bash
./scripts/update_weekly.sh --push ~/Downloads/0713周老素材.csv ~/Downloads/0713周新创意素材.csv
```

### 仅重建不上传

```bash
./scripts/update_account.sh
./scripts/update_weekly.sh
```

### 手动步骤

```bash
python3 scripts/build_static.py
git add data_inputs/ docs/
git commit -m "更新数据"
git push
```

push 后 Cloudflare/Netlify 约 **1 分钟**生效（GitHub Pages 视 Actions 状态可能更慢）。
