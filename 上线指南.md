# GrowMe-Database 上线指南（从零开始）

你的代码 **已经推送到 GitHub 了**。现在只差最后一步：打开网站开关。

---

## 当前状态

| 项目 | 状态 |
|------|------|
| GitHub 仓库 | ✅ 已有代码 https://github.com/sylvia-molan030/GrowMe-Database |
| 网站 Pages | ❌ 还没开（所以打开网址是 404） |

---

## 第一步：开启 GitHub Pages（只需做一次）

1. 打开：**https://github.com/sylvia-molan030/GrowMe-Database/settings/pages**

2. 找到 **Build and deployment**，按下面设置：

   - **Source** 选：`Deploy from a branch`
   - **Branch** 选：`main`
   - **文件夹** 选：`/web`（不是 root，一定是 web）
   - 点 **Save**

3. 等 1～3 分钟，页面顶部会出现绿色提示和网址。

4. 打开：**https://sylvia-molan030.github.io/GrowMe-Database/**

---

## 第二步：验证网站正常

网站打开后应能看到：
- 左侧 GrowMe 导航
- 素材黄金交叉复盘、智能排行榜等四个模块
- 侧边栏显示「GitHub 静态站」

如果页面空白：按 `Command + Shift + R` 强制刷新。

---

## 第三步：以后更新数据

每次有新 CSV/Excel：

```bash
cd /Users/sylvia/Desktop/sylvia/growme

# 1. 把新文件放进 data_inputs/

# 2. 重新生成网站数据
python3 scripts/build_static.py

# 3. 推送到 GitHub
git add data_inputs/ web/data/snapshot.json
git commit -m "更新数据"
git push
```

推送后等 1 分钟刷新网站即可。

---

## 常见问题

### push 时要密码？
- 用户名：`sylvia-molan030`
- 密码：填 GitHub **Token**（不是登录密码）
- Token 获取：https://github.com/settings/tokens → Generate new token (classic) → 只勾 **repo**

### 找不到 /web 文件夹选项？
说明代码还没推上去。在终端执行：
```bash
cd /Users/sylvia/Desktop/sylvia/growme
git push origin main
```

### 网站 404？
Pages 没开好。回到第一步，确认文件夹是 **/web** 不是 **/(root)**。

---

## 本地预览（可选）

```bash
cd /Users/sylvia/Desktop/sylvia/growme
./start.sh
# 浏览器打开 http://localhost:8000
```
